<h1 align="center">claude-checkpoint</h1>

<p align="center">
  <strong>Claude Code를 위한 세션 컨텍스트 영속화 도구</strong>
</p>

<p align="center">
  <a href="LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue.svg" alt="License: MIT"></a>
  <img src="https://img.shields.io/badge/platform-macOS%20%7C%20Linux-lightgrey.svg" alt="Platform">
  <img src="https://img.shields.io/badge/requires-Claude%20Code%20CLI-blueviolet.svg" alt="Requires Claude Code">
  <img src="https://img.shields.io/badge/shell-bash-green.svg" alt="Shell: Bash">
  <br>
  <a href="README.md"><img src="https://img.shields.io/badge/🇺🇸_English-white.svg" alt="English"></a>
  <a href="README.ko.md"><img src="https://img.shields.io/badge/🇰🇷_한국어-white.svg" alt="한국어"></a>
  <a href="README.ja.md"><img src="https://img.shields.io/badge/🇯🇵_日本語-white.svg" alt="日本語"></a>
</p>

<p align="center">
  Claude Code의 Hook 시스템을 활용하여 작업 상태를 자동으로 저장하고 복원합니다.<br>
  더 이상 새 세션마다 <em>"어디까지 했더라?"</em> 할 필요가 없습니다.
</p>

---

## 문제

Claude Code는 강력한 코딩 에이전트이지만, 컨텍스트 윈도우는 유한합니다.

- **Compact** — 컨텍스트가 커지면 요약으로 압축됩니다. 이 과정에서 파일 경로, 작업 체크리스트, 단계별 진행 상황 같은 구체적인 세부사항이 누락되는 경우가 많습니다
- **컨텍스트 윈도우 포화** — 컨텍스트가 완전히 꽉 차면 `/resume`으로도 해결이 안 됩니다. 작업 상태 없이 새 세션을 시작할 수밖에 없습니다
- **`/clear`** — 의도적으로 컨텍스트를 리셋하면 현재 작업 상태도 함께 사라집니다

장기 프로젝트에서는 작업이 길어질수록 중요한 진행 세부사항이 점점 유실됩니다.

## 해결책

`claude-checkpoint`는 Claude Code의 라이프사이클에 연결되어 **자동 세이브 포인트**를 생성합니다:

```
  평소처럼 작업
       │
       ▼
  git commit / 파일 편집 ──► 체크포인트 자동 저장
       │
       ▼
  세션 종료 (크래시, 종료, compact, /clear)
       │
       ▼
  새 세션 시작
       │
       ▼
  체크포인트가 컨텍스트에 복원 ──► Claude가 이어서 작업
```

설치 후 설정 불필요. 외울 명령어 없음.

## 빠른 시작

```bash
# 1. 클론
git clone https://github.com/Canto87/claude-checkpoint.git

# 2. 프로젝트에 설치
./claude-checkpoint/install.sh /path/to/your/project

# 3. 끝. 평소처럼 작업하세요.
```

> **요구사항:** [Claude Code CLI](https://docs.anthropic.com/en/docs/claude-code), `jq`, Git

## 작동 방식

### 이벤트 & 동작

| 이벤트 | 동작 |
|:------|:------|
| `git commit` | 체크포인트 저장 (완료 작업, 다음 할 일, 참조 문서) |
| 파일 편집/생성 | 체크포인트 자동 저장 (저장 간 10분 쿨다운) |
| 세션 시작 | 최근 체크포인트를 컨텍스트에 복원 |
| `/clear` | 최근 체크포인트를 컨텍스트에 복원 |
| Compact | 체크포인트 복원 + 미저장 상태 기록 요청 |

### 슬래시 커맨드

| 커맨드 | 설명 |
|:-------|:-----|
| `/save` | 수동으로 체크포인트 즉시 저장 |
| `/checkpoints` | 현재 브랜치의 체크포인트 목록 확인 |
| `/checkpoint-clear` | 오래된 체크포인트 파일 정리 |

### 파일 구조

체크포인트는 Claude Code의 auto-memory 디렉토리에 저장됩니다:

```
~/.claude/projects/{encoded-project-path}/memory/
├── MEMORY.md                        # 장기 지식 (사용자가 관리)
├── checkpoint-main-12345.md         # main 브랜치 세션 A
├── checkpoint-main-67890.md         # main 브랜치 세션 B
└── checkpoint-feat-auth-11111.md    # feature 브랜치 세션
```

- **MEMORY.md** — 프로젝트 패턴, 핵심 결정사항, 장기 메모
- **checkpoint-\*.md** — 현재 작업 상태, 다음 단계, 참조 문서 (자동 관리)

### 멀티 세션 안전성

각 세션은 고유한 체크포인트 파일에 기록합니다:

```
checkpoint-{브랜치}-{세션PID}.md
```

같은 프로젝트에서 병렬 세션이 충돌하지 않습니다. 복원 시 현재 브랜치의 **최근 3개** 체크포인트만 로드하여 컨텍스트 주입을 가볍게 유지합니다. 이전 체크포인트는 `/checkpoints`로 확인할 수 있습니다.

**24시간**이 지난 오래된 체크포인트는 자동으로 정리됩니다.

### 체크포인트 형식

```markdown
# Checkpoint

## Last Updated
- Date: 2025-01-15
- Commit: abc1234

## Completed Work
- 사용자 인증 모듈 구현 완료

## Current Roadmap Position
- Roadmap: docs/plans/ROADMAP.md
- Current step: Phase 2 - API 연동
- Status: in progress

## Next Task Checklist
- [ ] 토큰 갱신 로직 추가
- [ ] 통합 테스트 작성

## Reference Docs
- docs/plans/auth-design.md
- docs/API.md
```

## 아키텍처

```
┌──────────────────────────────────────────────────────────┐
│                     Claude Code CLI                      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  PostToolUse Hooks                                       │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Bash  → git commit 감지 → 체크포인트 저장          │  │
│  │  Edit  → 10분 쿨다운 자동 저장                      │  │
│  │  Write → 10분 쿨다운 자동 저장                      │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  SessionStart Hooks                                      │
│  ┌────────────────────────────────────────────────────┐  │
│  │  startup / clear / compact → 체크포인트 복원        │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  슬래시 커맨드                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │  /save  /checkpoints  /checkpoint-clear            │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  ~/.claude/projects/*/memory/                            │
│  ┌──────────────┐  ┌──────────────────────────────────┐  │
│  │  MEMORY.md   │  │  checkpoint-{branch}-{pid}.md    │  │
│  │  (장기 기억)  │  │  (단기 기억, 세션별)              │  │
│  └──────────────┘  └──────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 설치 / 제거

### 설치

```bash
./install.sh /path/to/your/project
```

수행 내용:
1. Hook 스크립트를 `.claude/hooks/`에 복사
2. 슬래시 커맨드를 `.claude/commands/`에 복사
3. Hook 설정을 `.claude/settings.json`에 병합 (기존 hooks 보존)
4. 프로젝트 `MEMORY.md`에 세션 프로토콜 섹션 추가

### 제거

```bash
./uninstall.sh /path/to/your/project
```

`claude-checkpoint` hook 항목, 커맨드, 임시 파일만 제거합니다. 다른 hooks와 설정은 건드리지 않습니다.

> `~/.claude/projects/*/memory/`의 체크포인트 파일은 **자동 삭제되지 않습니다.**
> 필요 없다면 수동으로 삭제하세요.

## 토큰 비용

Hook 스크립트 자체는 **순수 쉘 스크립트**로 **API 토큰을 소비하지 않습니다.**

토큰이 소비되는 경우:
- **세션 시작 시 체크포인트 복원** — 체크포인트당 ~200-500 토큰 (최대 3개 로드)
- **커밋 시 체크포인트 저장** — ~300-600 토큰
- **파일 편집 시 자동 저장** — ~300-600 토큰 (10분에 최대 1회)

일반적인 대화 사용량 대비 무시할 수 있는 수준입니다.

## FAQ

**Q: 커밋을 안 하면요?**
파일 편집 시 자동으로 체크포인트가 저장됩니다 (10분 쿨다운). `/save`를 실행하면 언제든 수동 저장도 가능합니다. 커밋하지 않아도 진행 상황이 주기적으로 기록됩니다.

**Q: 체크포인트 파일을 수동으로 편집할 수 있나요?**
네, 일반 Markdown 파일입니다. 단, 해당 세션에서 다음 자동 저장이나 커밋 시 덮어씌워집니다.

**Q: Worktree에서도 작동하나요?**
네. 각 worktree는 고유한 브랜치를 가지므로 체크포인트가 자연스럽게 분리됩니다.

**Q: `jq`가 설치되어 있지 않으면?**
설치 스크립트가 에러와 함께 종료됩니다. `brew install jq` (macOS) 또는 `apt install jq` (Linux)로 설치하세요.

**Q: 복원 시 몇 개의 체크포인트가 로드되나요?**
현재 브랜치의 최근 3개까지 로드됩니다. 이전 것들은 `/checkpoints`로 확인할 수 있습니다.

## 팁

### 체크포인트 업데이트를 조용하게 만들기

기본 상태에서는 hook으로 트리거된 체크포인트 저장이 Claude Code UI에 Edit/Write diff를 그대로 보여주며, 권한 승인을 요청할 수 있습니다.

체크포인트 업데이트가 한 줄 요약으로 접혀서 조용하게 처리되길 원한다면, `memory/` 경로에 대한 Edit/Write 권한을 자동 허용으로 설정하세요.

프로젝트별로 `.claude/settings.json`에서 설정할 수 있습니다:

```json
{
  "permissions": {
    "allow": [
      "Edit(memory/**)",
      "Write(memory/**)"
    ]
  }
}
```

이 설정은 선택 사항입니다 — 체크포인트 변경 내용을 매번 확인하고 싶다면 설정하지 않아도 됩니다.

## 기여

기여를 환영합니다! 이슈를 열거나 Pull Request를 보내주세요.

## 라이선스

[MIT](LICENSE)
